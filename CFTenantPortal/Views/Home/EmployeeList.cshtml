@model CFTenantPortal.Models.EmployeeListVM

@{
    ViewData["Title"] = "Employee List";
}

<h3>Employee List</h3>

@(await Component.InvokeAsync("EmployeeListX", new { employees = Model.Employees }))
