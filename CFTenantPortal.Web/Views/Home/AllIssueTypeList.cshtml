@model CFTenantPortal.Models.IssueTypeListVM

@{
    ViewData["Title"] = "Issue Types";
}

<h3>@Model.HeaderText</h3>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IssueTypes[0].Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IssueTypes[0].Description)
            </th>            
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.IssueTypes)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>              
                <td>
                    @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Issues", "AllIssueList", new { issueTypeId=item.Id })
                </td>
            </tr>
        }
    </tbody>
</table>
